# Design QA

Source visual: `/Users/qenawey/Downloads/IMG_8325.jpg`

Prototype route: `/header-concept`

Production route checked: `/`

Checks:
- The production homepage hero now adopts the approved circular reel orbit; the `/header-concept` route remains available as a reference preview.
- The prototype follows the reference composition: centered editorial headline, light studio background, angled floating media cards, a circular reel cluster, and CTA under the visual group.
- Real Scaleshift assets are used: brand logo and ten Cloudflare R2 portfolio reel videos.
- Browser verification confirmed the extra "Retention edits" and "60+ creators" cards are not rendered in the concept hero.
- Browser verification confirmed only five reel cards are visible at once, matching the reference's reduced visible count.
- Browser verification confirmed the reel cards move through a narrower masked upper semicircle, with lower-path cards hidden below the hero.
- Browser verification confirmed reel rotation now comes only from the circular orbit angle, with no extra perspective tilt animation.
- Browser verification confirmed the orbit center was lowered so reel entry and exit happen from the lower edge of the header section.
- Browser verification confirmed the orbit no longer renders a visible rectangular background; the fade is applied as a mask on the reel group.
- Browser verification against the Trend Studio / supplied screenshot direction confirmed the concept reel frames were enlarged substantially while keeping the same masked lower-entry orbit behavior.
- Browser verification confirmed the production homepage hero now uses the same ten-video orbit pattern and no longer renders the previous three `.attention-phone` reel elements.
- Browser verification confirmed no `3reels-in-header` sources remain in the homepage hero; the hero orbit uses ten `/portfolio/` R2 videos.
- Server-rendered HTML verification found 10 `.hero-orbit-reel` instances, 0 `.attention-phone` instances, and 0 `3reels-in-header` sources.
- The production homepage hero reel orbit now sits above the `Create Attention` copy block, with compact primary and work CTAs positioned naturally below the support copy.
- The production homepage no longer renders the hero retention-system pill or timeline strip, and the English hero CTA now reads "Build Retention Now".
- The production homepage no longer renders the horizontal proof ticker that included "600+ videos delivered".
- The production homepage hero stage uses `100svh` for the opening viewport, with the reel orbit separated from the copy block below it.
- The production homepage hero composition is constrained to fit inside the opening hero stage, and the reel orbit shadow is a subtle floor glow rather than a heavy blurred patch.
- The latest hero pass softens the red ambient glow, narrows the floor shadow under the reel orbit, and lowers the reel path so cards are not clipped harshly at the top.
- The homepage hero reel orbit no longer uses a clipping mask or hidden overflow on the orbit container, and its vertical radius is reduced so visible reel frames are not cropped.
- The homepage reel orbit has added top spacing from the navigation bar across desktop, tablet, and mobile breakpoints.
- The homepage hero content group is shifted downward slightly so the reel orbit and copy sit farther from the navigation bar together.
- The hero support copy now uses the 60+ clients / +1B views proof statement, with the final `System™` word visually emphasized in brand red.
- The hero support copy now also bolds the `60+` / `60` and `+1B` proof figures while keeping `System™` red.
- The homepage now renders the post-hero section set from `/Users/qenawey/Downloads/files/index.html`: trusted-by, comparison, framework, creator profiles, numbers, client feedback, selected work, results, FAQ, final CTA, and footer. The How to Get Started section and Process navigation link were removed in both languages (code-confirmed).
- The Retention Editing System framework now renders three video stages: Attention Hook™, Retention Structure™, and Watch Time Optimization™. Attention Layering™ and Growth Loop™ are removed in both languages. Desktop uses three centered columns, numbered 01-03, with two connectors and a 3.6-second animation cycle.
- Three-stage update checked at desktop and mobile widths: cards stay visible and connector transforms progress in the expected direction. English/Arabic content and numbering are code-confirmed; lint and production build passed.
- Framework sources replaced with the three matching files in R2 `Editing System/`, verified by authenticated bucket listing and HTTP 200 responses. Both languages use the same source mapping. Existing CSS hook findings are unrelated to this source-only change and remain unchanged.
- New framework playback browser-verified: all three sources reached readyState 2 or higher and were playing through the cached media endpoint. Lint and production build passed after the source update.
- Framework tiles stay visible, with a repeating red connector and moving arrow above the videos (vertical below 1025px). Browser-verified at 1440px and 390px: the line transform changes over time, all tile containers remain visible, and hero playback pauses after scrolling away. Stale development CSS was cleared by restarting with a fresh generated cache.
- Media loading is now owned by `ViewportVideo` across the complete source tree, including the concept header. Browser checks found 5 of 26 videos had sources on initial load, with no below-fold framework or selected-work sources loaded. `/api/media` returned HTTP 206, correct Content-Range, and a 24-hour Cache-Control header. Three automated checks cover range forwarding, cache/error handling, and shared-player ownership. Lint and production build passed. Browser cache reuse depends on retained cache entries and supported partial-response caching; no CDN hit-rate or production speed claim is made.
- Existing typography and corner-radius hook findings are outside this motion change; established visual values were retained without suppressing checks.
- Client feedback follows the inspected Scrollz What Clients Say reference: black grid surface, vertical review columns, central portrait story, segmented progress, navigation, play/pause and mute. The initial Scaleshift audio/photo prototype is superseded by 12 real videos in R2 `Client feedback/`. All 12 sources returned HTTP 200 and video/mp4. Desktop (1440px) and mobile (390px) browser verification confirmed portrait video decoding (1080x1920), muted playback, next-video navigation, unmute, pause, no horizontal overflow, and source removal/offscreen pause. Screenshots inspected. Lint, media invariant tests (3/3), and production build passed. Story radius 28px and gold review stars intentionally reflect the reference; older unrelated typography findings remain unchanged. No client names are inferred from numbered video filenames.
- Impact redesign: unboxed white metrics above two opposing rows of temporary reel JPEG stills on black. Portrait cards use 3:5 proportions and 8px corners. Hover/focus pauses the row, blurs and dims only the selected image, and displays an outlined centered Watch reel action. Desktop/mobile screenshots and click/Escape dialog flows verified. View counts remain 1B+/600+/96%/60+; no unsupported counts are attached to placeholder cards. Metric typography uses fixed 72/56/40px steps intentionally to fit changing digits; existing unrelated hook findings are retained.
- Impact verification: screenshots inspected at 1440px and 390px, no horizontal overflow or image errors. Arabic browser samples advanced from 0.00B+/0+/0%/0+ through intermediate values to 1B+/600+/96%/60+. Reduced-motion disables background animation. Lint, production build, and the three media ownership/cache tests passed. Images remain temporary pending the user's final uploads.
- Impact hover revision verified at 1440px/390px: portrait ratio approximately 0.6, selected-card blur/dimming and row pause confirmed; matching video dialog opens and closes with Escape. Shared dialog owner also replaces the selected-work modal. Full-tree invariant, lint, and build passed after this revision.
- Prior media playback verification (before reducing to three stages): all four framework videos reached readyState 3 or 4 and played. A repeated 1KB byte-range request transferred 1324 bytes on first access and 0 bytes on second access in Chrome, confirming browser cache reuse. Desktop and mobile motion screenshots were inspected; no production deployment was performed.
- The trusted-by proof section now uses the same black cinematic surface as the hero/header.
- The comparison section now uses centered Lucide check and X icons inside the status circles instead of raw text glyphs.
- The `Why ScaleShift?` comparison heading is slightly reduced from the shared reference-section display size for a better proportion with the comparison table.
- The `Why ScaleShift?` comparison cards no longer use a fixed minimum height, removing the unnecessary empty space below the bullet lists.
- The former standalone `Creators We Helped Grow` section has been removed from the homepage order; its client account data now appears as a horizontal slider directly under the trusted-by proof copy.
- The duplicate circular avatar strip above the trusted-by proof copy has been removed, leaving the account slider as the only client-image row in that section.
- The trusted-by client slider uses wide dark account pills with avatar, client name, Meta-style verified mark, Instagram icon, and follower count, matching the supplied account-card direction.
- Cloudflare R2 `scaleshift-content/clients-group/` was checked in the browser on 2026-09-07. The trusted-by account slider now uses the `Clients (1) 3d417c4b80778027b6c2d88c5914140b_all.csv` names and Instagram links, with all 20 matching R2 client images returning HTTP 200.
- The trusted-by account slider no longer uses a white avatar placeholder; visible-loop images load eagerly while the duplicated marquee set remains lazy.
- The trusted-by slider headline is now reduced to "Trusted by" / "موثوق من", and the account pills were tightened to a smaller, lighter layout.
- The trusted-by slider headline now includes a centered red Meta-style verified badge beside the label.
- The trusted-by account pills were reduced by roughly 30%, all displayed client names are English-only, names are constrained to one line, and every client card now shows the Meta-style verified badge.
- Long trusted-by client names now truncate inside their own text span, so the adjacent verified badge remains visible and unclipped.
- The repeated Scaleshift logo watermark has been removed from reference sections; the source reference's `logo.png` file was not present in `/Users/qenawey/Downloads/files`.
- The top navigation no longer links to the removed services section; it now points to sections that exist in the reference-based page structure.
- Browser verification confirmed the concept uses ten unique `/portfolio/` videos from the `scaleshift-content` R2 bucket and all videos autoplay without errors.
- Console verification found no browser errors.
- `npm run lint` passed.
- `npm run build` passed and includes `/header-concept`.

Known visual differences:
- The concept adapts the reference to Scaleshift's red/black identity and real reel assets instead of cloning the original community site's content.
- The preview is intentionally exploratory and should only replace the current hero after visual approval.

Final result: passed
- Removed the supporting invitation under footer Instagram in both languages and its unused CSS. Browser checks confirm the contact block contains only its heading, Instagram label and handle. Lint and four tests passed; links unchanged.
- Footer Explore navigation removed in both languages, including obsolete styles. Remaining brand/contact layout uses two columns on desktop and one on mobile. English 1440px and Arabic 390px browser checks confirm no footer navigation, contact retained, correct grid columns and no overflow. Lint, production build and four tests passed.
- Removed the footer brand tagline in English and Arabic, retaining the system label with explicit spacing below the logo. Browser checks confirm only the system label remains in the brand text in both languages. Lint, build and four tests passed; no links or interactions changed.
- Footer redesigned into brand, section navigation, and Instagram columns with separated copyright/language/back-to-top utilities. Mobile Arabic stacks the brand above two columns. Desktop and mobile screenshots inspected: logo decoded, all internal targets resolved, no horizontal overflow, and no invented contact/legal destinations. Lint, production build and four tests passed. Black/white/red palette and fixed 12-16px utility typography are intentional; unrelated legacy hook findings remain out of scope.
- FAQ numbering removed from the shared component; trigger grids now have question/icon columns only and answer padding aligns with questions in LTR/RTL. English desktop and Arabic mobile browser checks confirm no number elements, functioning expansion, two-column layout, and no overflow. Lint, production build, and four tests passed.
- FAQ redesigned as a shared single-open accordion, with original English/Arabic content, subtle dividers, full-width triggers, 48px/32px heading sizes, red numbering, and centered rotating Lucide plus icons. Desktop English and mobile Arabic browser tests passed: six collapsed questions initially, click opens, Enter switches the open panel, Space closes it, and no horizontal overflow. Closed panels are inert/aria-hidden and transitions respect reduced motion. Lint, build and four existing tests passed. New FAQ typography intentionally uses fixed sizes and zero tracking; unrelated legacy hook findings retained outside scope.
- Removed only the bottom testimonial seek slider shown in the user's screenshot, including its duration state and unused CSS. English desktop and Arabic mobile DOM checks confirm zero sliders, 12 top story segments, and two toolbar controls. Story counter and navigation retained. Lint and four tests passed.
- Consolidated results: removed the separate Real Results. Real Growth. section and old Instagram metric cards. Impact now has one row of 40 view screenshots and one row of six retained audience-retention charts, each with loop copies. Charts keep their full 758:519 aspect ratio and remain unblurred on hover. Desktop English and mobile Arabic verified two rows, 80 view elements, 12 chart elements, no old section, working #proof target inside impact, and no horizontal overflow. Screenshots inspected; lint, production build and four tests passed. Existing unrelated typography/color hook findings retained outside this scoped change; no new typography or palette introduced.
- Results asset integration: visually inspected all 40 R2 results/ images in four contact sheets and transcribed their visible counts (249K to 25.6M) into src/lib/result-screenshots.ts. Replaced temporary impact stills and unrelated video links; hover/focus now displays the matching number only, and links open original screenshots. English desktop 1440px and Arabic mobile 390px screenshots verified: 40 unique sources, 80 elements including loop copies, hovered image decoded, overlay opacity 1, row paused, and no document overflow. Images use contain to preserve count evidence. Lint, production build, three media tests, and the new screenshot manifest test passed. Existing portrait frame size and outlined hover treatment retained intentionally.
- Removed the central play/pause overlay and its unused CSS from the shared testimonial player. English desktop and Arabic mobile browser checks confirm no central button, with both top toolbar controls and the video retained. Lint and all three media tests pass.
- Removed the visible generic client-feedback heading from the shared story player and its unused heading CSS. Browser checks at English desktop and Arabic mobile confirmed zero story headings with video and progress controls retained. Lint and media tests passed.
- WhatsApp screenshot update: replaced text review cards with all seven original R2 screen-feedback images. All 14 rendered images (seven originals plus animation duplicates) decoded in browser verification. Images preserve 758:519 proportions without cropping, full-size links are available, hover/focus pauses movement, and duplicate links are not keyboard-focusable. Desktop 1440px and mobile 390px screenshots inspected, no horizontal overflow; original text cards removed from the source. Images load eagerly when this section enters the viewport to prevent offscreen carousel items staying unloaded. Lint, 3 media tests, and production build passed. Neutral screenshot borders and circular zoom affordance are intentional; the previous gold-star CSS was removed, while the existing 28px story radius is retained from the reference.
